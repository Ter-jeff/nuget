# nuget

Empty scaffold for a local NuGet feed. It previously repackaged legacy Office
primary-interop-assemblies (PIAs) so they could be consumed via
`PackageReference`, but that Office Interop dependency has been dropped
project-wide, so the packaging content has been removed.
